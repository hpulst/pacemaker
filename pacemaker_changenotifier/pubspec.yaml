name: pacemaker_changenotifier
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: 
  device_preview:
  package_info_plus: 
  percent_indicator: 
  provider: 
  shared_preferences: 
  url_launcher: 
  uuid:
  collection: ^1.15.0-nullsafety.4 

dev_dependencies:
  # test: 
  # integration_test:
  # flutter_test:
  #   sdk: flutter
  flutter_driver:
    sdk: flutter
  flutter_lints: ^1.0.4
  

flutter:

  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/json/
    - assets/
